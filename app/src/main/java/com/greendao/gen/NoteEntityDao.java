package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hua.note.data.NoteEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "NOTE_ENTITY".
*/
public class NoteEntityDao extends AbstractDao<NoteEntity, String> {

    public static final String TABLENAME = "NOTE_ENTITY";

    /**
     * Properties of entity NoteEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Flag = new Property(0, String.class, "flag", true, "FLAG");
        public final static Property Text = new Property(1, String.class, "text", false, "TEXT");
        public final static Property Time = new Property(2, String.class, "time", false, "TIME");
        public final static Property UserId = new Property(3, String.class, "userId", false, "USERID");
    }


    public NoteEntityDao(DaoConfig config) {
        super(config);
    }
    
    public NoteEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"NOTE_ENTITY\" (" + //
                "\"FLAG\" TEXT PRIMARY KEY NOT NULL ," + // 0: flag
                "\"TEXT\" TEXT," + // 1: text
                "\"TIME\" TEXT," + // 2: time
                "\"USERID\" TEXT);"); // 3: userId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"NOTE_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, NoteEntity entity) {
        stmt.clearBindings();
 
        String flag = entity.getFlag();
        if (flag != null) {
            stmt.bindString(1, flag);
        }
 
        String text = entity.getText();
        if (text != null) {
            stmt.bindString(2, text);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(3, time);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(4, userId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, NoteEntity entity) {
        stmt.clearBindings();
 
        String flag = entity.getFlag();
        if (flag != null) {
            stmt.bindString(1, flag);
        }
 
        String text = entity.getText();
        if (text != null) {
            stmt.bindString(2, text);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(3, time);
        }
 
        String userId = entity.getUserId();
        if (userId != null) {
            stmt.bindString(4, userId);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public NoteEntity readEntity(Cursor cursor, int offset) {
        NoteEntity entity = new NoteEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // flag
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // text
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // time
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // userId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, NoteEntity entity, int offset) {
        entity.setFlag(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setText(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setTime(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setUserId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final String updateKeyAfterInsert(NoteEntity entity, long rowId) {
        return entity.getFlag();
    }
    
    @Override
    public String getKey(NoteEntity entity) {
        if(entity != null) {
            return entity.getFlag();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(NoteEntity entity) {
        return entity.getFlag() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
